# Local Postgres credentials — this file is ignored by git
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gym_profile
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Connection string used by the app/migration runner
PGSSLMODE=disable
apiVersion: v1
kind: Service
metadata:
  name: gym-profile-service
  namespace: gym-dev
  labels:
    app: gym-profile-service
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
      name: http
  selector:
    app: gym-profile-service
